[project]
name = "project-indexer"
version = "0.1.0"
description = "Scan a directory tree for projects with READMEs and generate an interactive HTML index."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[dependency-groups]
# uv sync / uv run include this development group by default. The generated
# index and the scanner itself still have no third-party runtime dependencies.
dev = ["pytest>=8"]

[tool.uv]
# The indexer is a single standalone script, not an installable package — don't
# let uv try to build/install it into the project environment.
package = false

[tool.pytest.ini_options]
# Put the repo root on sys.path so tests can `import project_indexer`.
pythonpath = ["."]
testpaths = ["tests"]
